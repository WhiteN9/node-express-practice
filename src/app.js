const express = require("express");
const morgan = require("morgan");

const app = express();

// Middleware

// Application-level middleware

// Routes

// No-route handler

// Error handler

module.exports = app;
